
```shell
Next.js(App Router) + React18 + TypeScript + shadcn/ui(Radix) + Tailwind + TanStack Query + react-hook-form + zod + @tanstack/react-virtual + DOMPurify.
功能：订阅源管理(添加/删除/分类)、文章列表(无限滚动/虚拟列表)、已读/未读(单条+批量+未读数)、收藏、搜索(后端搜索+debounce)。
要求：组件拆分、query keys 设计、乐观更新、空状态/Skeleton/Toast、可访问性、暗黑模式可选。
输出：页面路由结构、组件代码、API 契约、状态流与缓存失效策略。
```