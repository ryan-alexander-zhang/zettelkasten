---
tags:
  - cheat-sheet
  - git
id: 20260110223334
created: 2026-01-10 22:33:34
status:
  - in_progress
type: cheat-sheet-note
aliases:
  - git
---

## Config

### Unset Config

```shell
git config --global --unset user.name
git config --global --unset user.email
```

## Worktree

```shell
# 给 feature/copilot 创建一个工作目录
git worktree add ../myproj-copilot feature/copilot

# 给 bugfix/test 创建一个工作目录
git worktree add ../myproj-test bugfix/test

# 移除 worktree
git worktree remove ../wt-feature
```

## Rebase

```shell
git rebase <branch>
```

## Checkout

```shell
git switch -c <branch> --track origin/<branch>
```

## Branch

```shell
git branch -a
git branch -r
```

## Stash

```shell
# -u untracked
git stash push -u -m "msg"

git stash list

git stash apply stash@{0}

# drop stash
git stash pop stash@{0}

git stash drop stash@{0}

# clear all
git stash clear

# new branch
# apply stash
git stash branch tmp-fix stash@{0}
```